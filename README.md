# (ทางเลือก) สร้างและเปิดใช้งาน Virtual Environment
python -m venv .venv
.\.venv\Scripts\activate  # สำหรับ Windows
source ./.venv/bin/activate  # สำหรับ macOS/Linux

pip install customtkinter google-genai pillow requests pyttsx3
